import { ReactComponent as IconLogoTitle } from '../../assets/images/ic-logo-title.svg';
import s from './brand.module.css';

const Brand = () => {
	return <IconLogoTitle className={s.logo__title} />;
};

export default Brand;
